# Delphi Broker — environment configuration
# Copy to .env and adjust for your deployment.

# Server
DELPHI_HOST=0.0.0.0
DELPHI_PORT=8420

# Database (SQLite)
# Relative paths are resolved from the project root.
# Docker override: /app/data/delphi.db (set in docker-compose.yml)
DELPHI_DB_PATH=delphi.db

# Web UI agent identity
DELPHI_WEB_UI_AGENT_ID=web-ui
DELPHI_WEB_UI_ROLES=orchestrator
